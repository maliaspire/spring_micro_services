server:
  port: ${sms.zuul_service.port}
eureka:
  instance:
    preferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: true
  serviceUrl:
    defaultZone: ${sms.eureka.defaultZone}

zuul:
  routes:
    usersService:
      path: /users-service/**
      serviceId: ${sms.user_service.service.name}
    ordersService:
      path: /orders-service/**
      serviceId: ${sms.orders_service.service.name}